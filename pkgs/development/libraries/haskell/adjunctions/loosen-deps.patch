Inspired by https://github.com/gentoo-haskell/gentoo-haskell/blob/7ae0b0d0167816a7eedc4db4e50716311e96a731/dev-haskell/adjunctions/adjunctions-3.2.1.1-r1.ebuild
diff --git a/adjunctions.cabal b/adjunctions.cabal
index ccadbfe..b4b4934 100644
--- a/adjunctions.cabal
+++ b/adjunctions.cabal
@@ -45,15 +45,15 @@ library
     transformers           >= 0.2     && < 0.4,
     mtl                    >= 2.0.1   && < 2.2,
     containers             >= 0.3     && < 0.6,
-    comonad                >= 3       && < 4,
+    comonad                >= 3       && < 5,
     contravariant          >= 0.2.0.1 && < 1,
     distributive           >= 0.2.2   && < 1,
-    semigroupoids          >= 3       && < 4,
+    semigroupoids          >= 3       && < 5,
     void                   >= 0.5.5.1 && < 1,
     keys                   >= 3       && < 4,
-    comonad-transformers   >= 3       && < 4,
+    comonad-transformers   >= 3       && < 5,
     representable-functors >= 3.1     && < 4,
-    free                   >= 3       && < 4
+    free                   >= 3       && < 5
 
   exposed-modules:
     Data.Functor.Adjunction
